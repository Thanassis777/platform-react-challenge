export const API_ENDPOINTS = {
  BREEDS: '/breeds',
  IMAGES: '/images',
  FAVORITES: '/favourites',
} as const;

export const ROUTES = {
  HOME: '/',
  BREEDS: '/breeds',
  IMAGE: '/image',
  FAVORITES: '/favorites',
  NOT_FOUND: '*',
} as const;
